{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Personel list</h1>

    <table class="records_list">
        <thead>
            <tr>
                <th>Id</th>
                <th>Imie</th>
                <th>Nazwisko</th>
                <th>Wiek</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>
                <td><a href="{{ path('personel_show', { 'id': entity.id }) }}">{{ entity.id }}</a></td>
                <td>{{ entity.imie }}</td>
                <td>{{ entity.nazwisko }}</td>
                <td>{{ entity.wiek }}</td>
                <td>
                <ul>
                    <li>
                        <a href="{{ path('personel_show', { 'id': entity.id }) }}">show</a>
                    </li>
                    <li>
                        <a href="{{ path('personel_edit', { 'id': entity.id }) }}">edit</a>
                    </li>
                </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

        <ul>
        <li>
            <a href="{{ path('personel_new') }}">
                Create a new entry
            </a>
        </li>
    </ul>
    {% endblock %}
